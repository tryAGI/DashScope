<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Label="Nuget">
    <PackageId>tryAGI.DashScope</PackageId>
    <Description>C# SDK for Alibaba Cloud Model Studio DashScope APIs, including Qwen text, multimodal, image, video, and embedding endpoints.</Description>
    <PackageTags>api;client;sdk;dotnet;openapi;dashscope;alibaba;model-studio;qwen;wan;llm;ai;multimodal;image-generation;video-generation;embeddings</PackageTags>
  </PropertyGroup>

</Project>

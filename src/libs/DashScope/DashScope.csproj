<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
  </PropertyGroup>

  <PropertyGroup Label="Nuget">
    <Description>C# SDK for Alibaba Cloud Model Studio DashScope APIs, including Qwen text, multimodal, image, and embedding endpoints.</Description>
    <PackageTags>api;client;sdk;dotnet;openapi;dashscope;alibaba;model-studio;qwen;llm;ai;multimodal;image-generation;embeddings</PackageTags>
  </PropertyGroup>

</Project>
